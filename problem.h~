#include <iostream>
#include <vector>
#include <string>

using namespace std;

struct problem
{
	string problem_name;
	int testcase_count, score_per_testcase, time_limit, memory_limit;
	string answer_suffix;
};
